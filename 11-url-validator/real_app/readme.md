# URL Dataset Cleaner

This tool validates a list of web addresses and classifies them as valid or invalid.

It was built from the logic of the World Wide Web coding challenge and applies the same validation rules to structured data.

## Features

- Loads URLs from a JSON file
- Validates each address
- Separates valid and invalid URLs
- Prints a simple validation report

## Run

```bash
python url_cleaner.py
